pluginManagement {
    repositories {
        mavenCentral()
        maven {
            url = uri("https://maven.pkg.github.com/plug-obp/plug-plugin")
            credentials {
                username = System.getenv("GITHUB_ACTOR") ?: "${GITHUB_ACTOR}"
                password = System.getenv("GITHUB_TOKEN") ?: "${GITHUB_TOKEN}"
            }
        }
    }
}

rootProject.name = 'obp2-collections'